# Dev-only override: point the frontend at a local Laravel backend.
# Loaded by `npm run dev` but NOT by `npm run build` (production keeps the
# deployed default in src/services/api.js).
VITE_API_BASE_URL=http://localhost:8000/api/v1
